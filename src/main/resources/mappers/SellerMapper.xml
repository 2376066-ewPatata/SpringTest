<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.irang.springtest.mvc.repository.SellerRepository">

    <insert id="insertSeller" parameterType="map">
        INSERT INTO
        `seller`
        (
             `nickname`
            , `profile_image`
            ,`temperature`
        )
        VALUE
        (
            #{nickname}
            , #{profileImage}
            , #{temperature}
        )
    </insert>

    <select id="selectLastSeller" resultType="com.irang.springtest.mvc.domain.Seller">
        SELECT
            `nickname`
             , `profile_image`
             ,`temperature`
        FROM
            `seller`
        ORDER BY
            id DESC
        LIMIT 1
    </select>

    <select id="selectSellerId" parameterType="int">
        SELECT
            `nickname`
             , `profile_image`
             ,`temperature`
        FROM
            `seller`
        WHERE
            id = #{id}
    </select>


</mapper>